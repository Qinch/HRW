qinchao
2014-10-22 15:27:19

第一个参数为宽高比，第二个参数为输入文件名;
注意：
	./backup/bin2txt.cpp为将二进制的out10.bin转换成文本文件out10.txt的程序，用于验证写入的二进制文件是否正确。
	 运行命令: ./bin2txt  out10.bin 1000 其中第0个参数为执行程序名，第1个参数为待转换的二进制文件名，
	 第2个参数可选,默认情况下，全部转换；如果过设置1000，则表示只转换转移区域表的前多少个。
	 第3个参数为输出的文本文件对应的文件名。

